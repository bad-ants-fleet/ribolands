""" * * * * * * TODO * * * * * *
  *) make a python module out of this 

      ViennaRNA
      -> import RNA as vrna

      Python Libraries
      -> ribolands
      -> ribolands.syscalls
      -> ribolands.walks

      Scripts/Tests:
      -> BarMap.py
      -> DrTransformer.py
      -> cofold_barriers.py

      depricated, repace with softconstraints:
      -> spatch.py

  *) catch import errors if RNA, RNAsubopt, barriers and treekin don't exist

  *) make barriers work in its own temporary directory 
    and *then* rename the standard file names (rates.out)

  *) think of an object oriented intereface to specify model details
  *) think of naming I/O files (*.bar, *.spt, *.spt.gz, *.tkn, *.rts, *.ps, *.pdf ...)
  *) write documentation (sphinx, maybe switch to google-style)
  *) wrtie a preamble for every lib-file
  *) write test files

  *) compare spatch-hack with interkin 
  *) make an spatch decorator function for sys_suboptimals

"""

